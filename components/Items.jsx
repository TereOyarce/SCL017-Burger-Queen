import React from 'react'
import Menu from './Menu'

 const Items = ({item, cart ,setCart , actuallyData}) => {
  const {info,img,price}= item
    return (
        <div>
        {item}    
        </div>
    )
}
export default Items